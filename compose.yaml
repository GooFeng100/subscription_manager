services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: subscription-manager-app:latest
    container_name: subscription-manager-app
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-3000}
      APP_BASE_URL: ${APP_BASE_URL:-http://192.168.10.3:8084}
      MONGODB_URI: ${MONGODB_URI:-mongodb://mongodb:27017/subscription_manager}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      SESSION_SECRET: ${SESSION_SECRET:-change-me-before-start}
      SESSION_COOKIE_NAME: ${SESSION_COOKIE_NAME:-sm_session}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-false}
      LOGIN_FAIL_LIMIT: ${LOGIN_FAIL_LIMIT:-5}
      LOGIN_LOCK_MINUTES: ${LOGIN_LOCK_MINUTES:-15}
      REGISTER_IP_LIMIT: ${REGISTER_IP_LIMIT:-10}
      REGISTER_IP_WINDOW_MINUTES: ${REGISTER_IP_WINDOW_MINUTES:-60}
      SUB_RATE_LIMIT_PER_MINUTE: ${SUB_RATE_LIMIT_PER_MINUTE:-60}
      UPSTREAM_POLL_INTERVAL_MINUTES: ${UPSTREAM_POLL_INTERVAL_MINUTES:-60}
      SUB_CONVERTER_TIMEOUT_MS: ${SUB_CONVERTER_TIMEOUT_MS:-10000}
      UPSTREAM_FETCH_PROXY_URL: ${UPSTREAM_FETCH_PROXY_URL:-}
      REGISTRATION_ENABLED: ${REGISTRATION_ENABLED:-true}
      LOGIN_TURNSTILE_ENABLED: ${LOGIN_TURNSTILE_ENABLED:-true}
      REGISTER_TURNSTILE_ENABLED: ${REGISTER_TURNSTILE_ENABLED:-true}
      TURNSTILE_ENABLED: ${TURNSTILE_ENABLED:-false}
      TURNSTILE_SITE_KEY: ${TURNSTILE_SITE_KEY:-}
      TURNSTILE_SECRET_KEY: ${TURNSTILE_SECRET_KEY:-}
      CONVERTER_BACKEND_URL: ${CONVERTER_BACKEND_URL:-http://subconverter:25500/sub}
      CONVERTER_SOURCE_SECRET: ${CONVERTER_SOURCE_SECRET:-change-me-internal-source-secret}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123456}
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - subscription-manager

  subconverter:
    image: tindy2013/subconverter:latest
    container_name: subscription_manager_subconverter
    restart: unless-stopped
    expose:
      - "25500"
    networks:
      - subscription-manager

  caddy:
    build:
      context: .
      dockerfile: caddy/Dockerfile
      args:
        VITE_APP_BASE_URL: ${APP_BASE_URL:-http://192.168.10.3:8084}
    image: subscription-manager-caddy:latest
    container_name: subscription-manager-caddy
    restart: unless-stopped
    ports:
      - "8084:80"
    depends_on:
      - app
    networks:
      - subscription-manager

  mongodb:
    image: mongo:7
    container_name: subscription-manager-mongodb
    restart: unless-stopped
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - subscription-manager

  redis:
    image: redis:7-alpine
    container_name: subscription-manager-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - subscription-manager

volumes:
  mongodb_data:
  redis_data:

networks:
  subscription-manager:
    driver: bridge
